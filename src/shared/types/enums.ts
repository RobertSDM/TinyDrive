export enum NotifyLevel {
    info,
    error,
}

export enum ItemType {
    FOLDER = "FOLDER",
    FILE = "FILE",
}

export enum HTTPMethods {
    POST = "POST",
    GET = "GET",
    PUT = "PUT",
    DELETE = "DELETE",
}

export enum ProjectMode{
    PROD = "prod",
    DEV = "dev"
}
