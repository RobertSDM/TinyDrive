export enum NotificationLevels {
    INFO = "info",
    ERROR = "error",
}

export enum NotificationTypes {
    STATIC = "static",
    DYNAMIC = "dynamic",
}

export enum ItemType {
    FOLDER = "FOLDER",
    FILE = "FILE",
}
